@model List<Productos>

<h1>Lista de Productos</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var producto in Model){
            <tr>
                <td>@producto.IdProducto</td>
                <td>@producto.Descripcion</td>
                <td>@producto.Precio</td>
                <td>
                    <a asp-controller="Productos" asp-action="ModificarProductos" asp-route-id="@producto.IdProducto" class="btn btn-secondary"> Modificar</a>
                    <a asp-asp-controller="Productos" asp-action="EliminarPrductos" asp-route-itemid="@producto.IdProducto" class="btn btn-danger"> Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="text-center">
    <a asp-asp-controller="Producots" asp-action="AltaProducto" class="btn btn-primary">Agregar Producto</a>
</div>